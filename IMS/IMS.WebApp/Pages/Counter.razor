@page "/counter"

@inject IViewInventoriresByNameUseCase ViewInventoriresByNameUseCase

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>
<br />
<ViewInventoriesComponent OnSearchInventories="OnSearchInventories"></ViewInventoriesComponent>


@code {
    private int currentCount = 0;

    protected override async Task OnInitializedAsync()
    {
        var invs = await ViewInventoriresByNameUseCase.ExecuteAsync("ngi");
    }

    private void IncrementCount()
    {
        currentCount++;
    }

    private void OnSearchInventories(List<Inventory> inventories)
    {
        
    }
}
